import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { useState } from 'react';
import { Input } from '../../src/index'; import {Preview} from '@storybook/addon-docs';

<Meta
  title="Components/Input"
  parameters={{
    viewMode: 'docs',
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
  component={Input}
/>

## Inputs

An input is a basic html form element.

<Story name="Basic Usage">
  {args => {
    const [value, setValue] = useState(args.value);
    const handleChange = (e) => {
      setValue(e.target.value);
    };
  return (
    <Input
      htmlId="example"
      name="example"
      value={value}
      onChange={(e)=>handleChange('onChange', e)}
      aria-label={"Example"}
      {...args}
    />
  )}}
</Story>

<ArgsTable story="Basic Usage" />

### Inline

Inputs can also be display inline.

<Preview>
  <Story name="Inline">
    <Input id="inline" name="inline" aria-label={"Inline"} inline />
  </Story>
</Preview>
