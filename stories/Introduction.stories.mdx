import { Meta } from '@storybook/addon-docs/blocks';

<Meta
  title="Introduction"
  parameters={{
    viewMode: 'docs',
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
/>

## Storybook

Storybook provides a ui which can be used by

* Designers
* Developers
* QA

<p style={{color: 'red'}}>Using MDX, html can also be directly added here with inline styling</p>
